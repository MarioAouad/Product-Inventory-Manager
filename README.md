# Product-Inventory-Manager
A lightweight desktop application built with Python and Tkinter for managing product inventories. It provides a user-friendly GUI to perform CRUD (Create, Read, Update, Delete) operations, enforce data validation, and automatically export database records to SQL files for backup or migration.
